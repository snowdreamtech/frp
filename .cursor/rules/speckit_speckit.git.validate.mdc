---
description: Proxy for the speckit.git.validate workflow
globs: *
---

# Speckit Workflow: speckit.git.validate

When the user asks to run `/speckit.git.validate`, you MUST read `.specify/commands/speckit.git.validate.md` and follow its instructions exactly.
